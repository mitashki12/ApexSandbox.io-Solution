public String nameFromEmail(String email) {
    //code here
    List<String> splitedString = email.split('[@.]');
    String firstName = splitedString[0].toLowerCase();
    String lastName = splitedString[1].toLowerCase();
    return firstName.capitalize() + ' ' + lastName.capitalize();
}