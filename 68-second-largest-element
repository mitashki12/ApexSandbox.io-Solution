public Integer secondLargest(List<Integer> numbers) {
    //code here
    Integer currentLargestNumber = numbers[0];
    Integer secondLargestNumber = numbers[0];
    for(Integer i=1;i<numbers.size();i++){
        if(numbers[i]>currentLargestNumber){
            currentLargestNumber=numbers[i];
        }
    }
    for(Integer j=1;j<numbers.size();j++){
        if(numbers[j] == currentLargestNumber){
            continue;
        }
        if(numbers[j]>secondLargestNumber&&numbers[j]==currentLargestNumber){
            secondLargestNumber=numbers[j];
        }
    }
    return secondLargestNumber;
}